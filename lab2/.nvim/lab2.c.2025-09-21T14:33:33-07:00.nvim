#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int main() {
  char *line = NULL;
  size_t len = 0;
  ssize_t theLine;

  while (1) {
    printf("Enter programs to run\n");
    theLine = getline(&line, &len, stdin);
    printf("read in: %s", line);
  }
}
